
/**
 * Функция-обработчик для инициализации объекта скриптового снаряда.
 * См. пример в "bullet-custom-example.ts"
 */
function scriptBulletTemplate_initialize(bull: BaseBullet, emitArgs: BulletEmittingArgs) {
	logw("Bullet initialize template stub");
}

/**
 * Функция-обработчик скриптового снаряда. Вызывается каждый такт.
 * См. пример в "bullet-custom-example.ts"
 */
function scriptBulletTemplate_process(bull: BaseBullet) {
	//logw("Bullet process template stub");
}
